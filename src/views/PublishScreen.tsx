import React from "react";
import {SafeAreaView} from "react-native";

export function PublishScreen() {
  return (
    <SafeAreaView style={standardStyles.container}>
      <ScrollDiv flex={1} bg="white" px="lg" py="sm"></ScrollDiv>
    </SafeAreaView>
  );
}
